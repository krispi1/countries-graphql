import React from 'react';

function Country() {
  return (
    <div>
      <h2>Country</h2>
    </div>
  );

} // end Country()

export default Country;

// `/countries/(:code)` must render the currency and a area 
// code (phone) of that country.